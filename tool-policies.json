{
  "version": "2.0",
  "tools": {
    "validate_definition": {
      "level": "STORAGE",
      "relaxedFields": ["yaml"],
      "description": "Validates YAML content - may contain code-like structures"
    },
    "create_definition": {
      "level": "STORAGE",
      "relaxedFields": ["yaml", "description", "tags"],
      "description": "Creates definitions with YAML content that may contain code patterns"
    },
    "update_definition": {
      "level": "STORAGE",
      "relaxedFields": ["yaml", "description", "tags"],
      "description": "Updates definitions with YAML content that may contain code patterns"
    },
    "render_definition": {
      "level": "QUERY",
      "relaxedFields": [],
      "description": "Renders definition markdown"
    },
    "upgrade_definition": {
      "level": "STORAGE",
      "relaxedFields": ["yaml", "source_format"],
      "description": "Upgrades legacy YAML format - may contain code patterns"
    },
    "deprecate_definition": {
      "level": "STORAGE",
      "relaxedFields": ["reason", "successor"],
      "description": "Deprecation reason may contain technical references"
    },
    "fork_definition": {
      "level": "STORAGE",
      "relaxedFields": ["new_name", "description"],
      "description": "Fork definition with user-provided names"
    },
    "update_and_publish": {
      "level": "STORAGE",
      "relaxedFields": ["yaml", "description", "tags"],
      "description": "Composite update+publish with YAML content that may contain code patterns"
    },
    "batch_publish": {
      "level": "QUERY",
      "relaxedFields": [],
      "description": "Batch publish multiple definitions by reference"
    },
    "record_execution": {
      "level": "STORAGE",
      "relaxedFields": ["idempotency_key"],
      "description": "Records execution with idempotency key"
    },
    "get_user": {
      "level": "QUERY",
      "relaxedFields": ["id"],
      "description": "User ID lookup - UUID format can trigger false positives"
    },
    "batch_users": {
      "level": "QUERY",
      "relaxedFields": ["ids"],
      "description": "Batch user IDs - UUIDs can trigger false positives"
    }
  },
  "defaultLevel": "QUERY"
}
